version: "3"
services:
  temporal_tctl:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3778:3778"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
    networks:
      - temporal-network
networks:
  temporal-network:
    name: temporal-network
    external: true